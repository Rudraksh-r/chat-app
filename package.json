{
  "name": "chat-app",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time chat application (monorepo root — dev tooling only)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
