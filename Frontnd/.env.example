# Backend API base URL (must match your backend's address + /api prefix)
VITE_API_BASE_URL=http://localhost:5001/api

# WebSocket server URL (must match your backend's address, no path)
VITE_SOCKET_URL=http://localhost:5001
